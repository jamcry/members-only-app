<%= provide(:title, "New Post") %>
<h1>Create a Post</h1>
<div class="row">
  <% if !@post.errors.empty? %>
    <% messages = @post.errors.full_messages %>
    <p>There are <%= pluralize(messages.length,'error') %> in the submitted form: </p>
    <ul class="list-group small">
      <% messages.each do |msg| %>
        <li class="list-group-item list-group-item-danger"><%= msg %></li>
      <% end %>
    </ul>
  <% end %>
</div>
<div class="row">
  <div class="col-md-10 col-md-offset-3 center-form">
    <%= render 'form' %>
  </div>
</div>